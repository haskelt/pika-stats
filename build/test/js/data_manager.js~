import means_table from '{{SITE_PATH}}/js/means-table/means-table.js';

class DataManager {

    /*------------------------------------------------------------------------*/

    static regenerateData () {

	for(let col in values){
	    for(let row in values[col]){
		this.cells[col][row].innerText = values[col][row];
	    }
	}
	
    } // setCellValues
    
    /*------------------------------------------------------------------------*/
    
} // DataManager

export default DataManager;
